<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
     <% if Helpers.is_logged_in?(session) %>
      <h1>Welcome <%= session[:user_id] .username %></h1>
      <h3>Your Balance: <%= session[:user_id].balance %></h3>
      <h4><a href="/logout">Log Out</a></h4>
    <% end %>

  </body>
</html>